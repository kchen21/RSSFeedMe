<% layout('homepage_layout') -%>

<h1>Feeds</h1>

<ul class="feeds">
  <% feeds.forEach((feed) => { %>
    <li>
      <img src="<%= feed.image_url %>"/>
      <div class="info">
        <h2><%= feed.title %></h2>
        <p><%= feed.description %></p>
      </div>
    </li>
  <% }); %>
</ul>
